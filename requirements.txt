# Core project dependencies (Phase 1 - Object Removal)
# Used with requirements-gsplat.txt in setup.sh for steps 00-05c
torch

# Diffusion models
diffusers
transformers
accelerate

# LaMa inpainting
simple-lama-inpainting

# Mask generation
groundingdino-py
# SAM2 installed via setup.sh