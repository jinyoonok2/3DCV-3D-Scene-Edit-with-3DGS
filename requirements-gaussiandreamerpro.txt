# GaussianDreamerPro Dependencies
# Two-stage text-to-3D generation with mesh-bound Gaussians
# Based on: https://github.com/hustvl/GaussianDreamerPro

# Core dependencies (will be installed via conda/pip in setup script)
# torch==2.0.1 with CUDA 11.8
# pytorch3d (from wheels)

# Python packages
omegaconf
einops
trimesh
pyglet<2
open3d
plyfile
scikit-image
opencv-python
diffusers
transformers
accelerate
xformers
imageio
imageio-ffmpeg
lpips

# Shap-E dependencies
matplotlib
Pillow
tqdm
fire
humanize
requests
filelock

# For background removal (optional)
rembg[gpu]==2.0.59

# Note: Custom CUDA kernels built separately:
# - diff-gaussian-rasterization (from GaussianDreamerPro/submodules)
# - diff-gaussian-rasterization_2dgs
# - simple-knn
