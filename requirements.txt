# Core dependencies for InstructGS
torch>=1.12.0
pyyaml>=6.0
numpy>=1.21.0,<2.0  # pycolmap requires numpy<2.0
Pillow>=9.0.0
tqdm>=4.64.0
opencv-python>=4.5.0
imageio>=2.9.0

# For interactive interface (optional but recommended)
rich>=12.0.0

# For Stable Diffusion Inpainting (Module 05-06)
diffusers>=0.21.0
transformers>=4.21.0
accelerate>=0.20.0

# For LaMa Inpainting (Module 05b - Alternative to SDXL)
simple-lama-inpainting>=0.1.0

# For TripoSR (Module 06 - Image to 3D)
tsr
trimesh>=3.20.0
rembg[gpu]>=2.0.50

# For Depth Anything v2 (Module 07 - Depth estimation)
# Loaded via Transformers from HuggingFace: depth-anything/Depth-Anything-V2-Large-hf
# No additional installation needed - models download on first use

# For perceptual loss and evaluation
lpips>=0.1.4
# CLIP: Install via: pip install git+https://github.com/openai/CLIP.git
# (installed by setup.sh)

# For text-to-mask generation (Module 03)
groundingdino-py
# SAM2 installed via git: pip install git+https://github.com/facebookresearch/segment-anything-2.git

# For visualization
matplotlib>=3.5.0

# For gsplat (already in gsplat-src requirements)
# These are needed if you want to install separately
# gsplat
# torch-scatter
git+https://github.com/rmbrualla/pycolmap@cc7ea4b7301720ac29287dbe450952511b32125e  # Required for COLMAP dataset loading
scikit-learn  # Required for gsplat examples
viser  # For visualization
git+https://github.com/nerfstudio-project/nerfview@4538024fe0d15fd1a0e4d760f3695fc44ca72787
imageio[ffmpeg]
torchmetrics[image]
tyro>=0.8.8
tensorboard
tensorly
# git+https://github.com/rahul-goel/fused-ssim@328dc9836f513d00c4b5bc38fe30478b4435cbb5  # Optional - causes build issues, requires PyTorch in isolated build env
git+https://github.com/harry7557558/fused-bilagrid@90f9788e57d3545e3a033c1038bb9986549632fe
splines