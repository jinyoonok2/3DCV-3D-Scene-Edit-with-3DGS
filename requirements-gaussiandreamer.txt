# GaussianDreamer image-to-3D Gaussian generation dependencies (Phase 2)
# This file is used independently in setup-generation.sh
# Note: PyTorch, numpy, Pillow installed separately in setup script
# Note: tiny-cuda-nn installed separately (needs torch for build)
# Note: threestudio installed separately with --no-deps for isolation

# Core 3D generation framework
omegaconf
einops
xformers  # Will use PyPI since cu121 index removed

# Background removal
rembg[gpu]==2.0.59
onnxruntime

# tiny-cuda-nn (required by threestudio)
git+https://github.com/NVlabs/tiny-cuda-nn/#subdirectory=bindings/torch

# Background removal
rembg[gpu]==2.0.59
onnxruntime

# 3D processing
trimesh
pyglet<2
libigl  # Python bindings for libigl (geometry processing)

# Image downloading
gdown

# Additional dependencies for GaussianDreamer
pytorch-lightning
configargparse
